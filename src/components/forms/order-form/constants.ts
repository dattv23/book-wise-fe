/* eslint-disable no-unused-vars */
const INFO_FIELDS = [] as const

const DETAILS_FIELDS = [] as const

enum PageTitle {
  edit = 'Chỉnh sửa thông tin đơn hàng',
  add = 'Thêm đơn hàng mới'
}

export { INFO_FIELDS, DETAILS_FIELDS, PageTitle }
